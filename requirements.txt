# ─────────────────────────────────────────────────────────────
# Requirements — Scoliosis X-Ray Classifier
# Konfigurasi: Google Drive + .keras (sementara, tanpa TFLite)
#
# Perubahan utama dari versi lama:
#   HAPUS : jax[cpu]        ← penyebab reboot lambat (~800MB)
#   HAPUS : keras standalone ← sudah include di tensorflow
#   TAMBAH: tensorflow-cpu   ← backend pengganti, lebih ringan
# ─────────────────────────────────────────────────────────────

streamlit>=1.35.0
numpy>=1.26.0
Pillow>=10.0.0
matplotlib>=3.7.0
requests>=2.28.0
gdown>=5.1.0
tensorflow-cpu>=2.15.0,<2.17.0
